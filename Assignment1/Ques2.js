/*
Write the pseudocode & a program to calculate the simple interest based on the
inputs(amount, rate, time) provided by the user.
*/

/* PseudoCode
start 
input p, r, t
si <- p * r * t / 100
print si
end
*/

let p = 1000, r = 10, t = 1;
let si = p * r * t / 100
console.log(si);